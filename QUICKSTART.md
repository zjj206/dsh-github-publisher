# 一句话发布 GitHub

## 安装后怎么用

1. 在 DSH Web 选择包含 `github-publisher` 工具行的用户 Agent Preset。
2. 新开一个会话。
3. 对模型说：

   > 把当前项目发布到 GitHub。

插件自动选择：当前项目目录、当前 GitHub 登录账号、项目文件夹名作为仓库名、公开仓库、当前分支（或 main）以及 `v0.1.0` 版本。

4. 阅读显示的发布确认单。它会列出项目路径、仓库地址、文件数、分支、可见性、版本和敏感文件扫描结果；此时尚未上传。
5. 内容无误后只回复：

   > 确认发布。

插件才会初始化/提交 Git、创建或连接 GitHub 仓库、推送项目，并创建 GitHub Release。

## 需要自定义时

直接补充一句即可：

- `把当前项目发布到 GitHub，仓库叫 my-demo，私有发布，版本 v1.2.0。`
- `把 D:\projects\website 发布到 GitHub，仓库名 company-site，公开发布，版本 v2.0.0。`
- `给 alice/demo 创建 v1.1.0 Release，说明是修复登录问题。`

不需要输入 GitHub Token，也不需要记住任何工具名。

## 发布前仍应检查

插件会阻止常见 `.env`、私钥与云凭据文件，但不能识别源代码中的任意硬编码密钥。确认前请检查确认单和项目的 `.gitignore`。
